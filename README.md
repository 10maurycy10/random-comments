this programs require ~10000 messages in ``big.txt``.

the ``extract.js`` script can generate the contence for the file, given a json matrix dump.

then run ``node predict.js`` to generate random comments
